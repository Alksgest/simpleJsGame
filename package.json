{
    "name": "name",
    "description": "description",
    "authors": "author",
    "version": "1.0.0",
    "main": "main",
    "dependencies": {},
    "type": "module",
    "scripts": {
        "start": "node build.js --start",
        "build": "tsc && node build.js --build",
        "rbuild": "tsc && node build.js --build --start"
    }
}